export const organizations = state => state.organizations;
export const organization = state => state.organization;
export const organizationClients = state => state.organizationClients;

export default {
  organizations,
  organization,
  organizationClients,
};
