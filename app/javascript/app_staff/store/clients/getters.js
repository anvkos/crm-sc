export const clients = state => state.clients;
export const client = state => state.client;
export const clientOrganizations = state => state.clientOrganizations;

export default {
  clients,
  client,
  clientOrganizations,
};
